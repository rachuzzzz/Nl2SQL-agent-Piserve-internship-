# ============================================================
# NL2SQL Pipeline — Database & Model Configuration
# ============================================================
# Copy this file to .env and fill in your actual values:
#   copy .env.template .env
# ============================================================

# PostgreSQL Connection
DB_HOST=192.168.2.190
DB_PORT=5432
DB_NAME=form_builder
DB_USER=myuser
DB_PASSWORD=mypass
DB_SCHEMA=public

# Ollama Configuration
OLLAMA_URL=http://localhost:11434
OLLAMA_SQL_MODEL=qwen2.5:14b
OLLAMA_CHAT_MODEL=mistral:latest

# Embedding Model (runs on CPU, ~130MB download on first run)
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
